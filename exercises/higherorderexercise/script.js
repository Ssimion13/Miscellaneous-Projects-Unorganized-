 var cars = ["car", "truck", "dennis", "a"]

/* for (var i = 0; i < cars.length; i++) {
  console.log(`${cars[i]} goes vroom`);
}

var beasts = cars.forEach(function(x){
  console.log(x + " goes vroom");
})


var angryCars = [];

function upperCaseCars(arr){
  return arr.map(function(x){
    return x.toUpperCase();
  }
)
}

angryCars = upperCaseCars(cars);
console.log(angryCars);



var newCars = [];
function newerCars(arr){
  return arr.filter(function(x){
    return x.length > 3;
  })
}


newCars = newerCars(cars);
console.log(newCars);



function hasA(arr){
  return arr.find(function(x){
    if( x === "a")
    return cars;
  }
)
}

console.log(hasA(cars));


*/
