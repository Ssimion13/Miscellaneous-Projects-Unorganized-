import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

const names = ["World!", "Ryan", "Kashta", "Luke", "Liz"];

ReactDOM.render( <App/ > , document.getElementById('root'));

//render is a function  for React.DOM that connects react to HTML.
